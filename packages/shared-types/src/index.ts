// @hourtrack/shared-types -- domain type definitions.
// Concrete entity types (Card, Entry, Settings, DriveSnapshot) land in S02
// per docs/PROJECT_PLAN.md §7. This S01 export keeps the workspace resolvable
// so apps/web can import the package without errors before S02 fills it in.

export {};
