// @hourtrack/shared-utils -- shared pure utilities (earnings, duration, date-range).
// Concrete implementations land in S02 per docs/PROJECT_PLAN.md §7.2-§7.3.
// This S01 export keeps the workspace resolvable so apps/web can import it
// without errors before S02 fills it in.

export {};
